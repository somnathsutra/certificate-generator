# certificate-generator
A bulk certificate generator that creates personalized PDF certificates for every name in a file. Supports custom templates, font styles, colors, and layout adjustments. Easily generate high-quality certificates for events, workshops, and programs with a fast, user-friendly workflow.
